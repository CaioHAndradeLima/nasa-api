export class NasaPicture {
    constructor(
        public date: string,
        public explanation: string,
        public title: string,
        public url: string,
        public id: string
    ) {
    }
}
